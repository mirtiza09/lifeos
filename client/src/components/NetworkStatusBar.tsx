// This component has been removed as part of app simplification
// We no longer need to track network status since we're not supporting offline mode

export default function NetworkStatusBar() {
  return null;
}