# Example environment variables for Life OS

# Supabase connection settings (if needed in the future)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=your-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# DEFAULT_USER_ID=user-uuid

# Application settings
CREATE_DEMO_USER=true

# Default passcode for the application
# This will be used as the initial passcode for authentication
# In production, set this to a secure 4-digit value
DEFAULT_PASSCODE=6969

# For client-side access (used in offline mode)
# Note: Variables must be prefixed with VITE_ to be available in the frontend
VITE_DEFAULT_PASSCODE=6969