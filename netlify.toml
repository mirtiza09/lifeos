[build]
  command = "bash build-for-netlify.sh"
  publish = "dist/public"
  functions = "netlify/functions"

# Handle API routes
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

# SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200